spring:
  mail:
    host: localhost
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
  flyway:
    enabled: true

app:
  cors:
    allowed-origins:
      - http://localhost:3000
      - http://localhost:3001
