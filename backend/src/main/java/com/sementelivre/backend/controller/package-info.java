/**
 * Camada de entrada da API REST ({@code @RestController}). Recebe requisições
 * HTTP, valida os DTOs de entrada e delega a lógica de negócio à camada
 * {@link com.sementelivre.backend.service}. Não deve conter regra de negócio.
 */
package com.sementelivre.backend.controller;
